<p id="notice"><%= notice %></p>

<table class="table table-hover table-stripped">
  <tbody>
  <% if not @order.pizzas.nil? and not @order.pizzas.blank? %>
    <%@order.pizzas.each do |pizza| %>
  <tr>
    <td><img src="/assets/pizza.jpeg"></td>
    <td>Pizza: <% Recipe.find(pizza.recipe_id).name %></td>
    <td>Crust: <%=  Crust.find(pizza.crust_id).regorthin %></td>
  </tr>
    <% end %>
  <% end %>
  </tbody>
</table>

<p>

</p>

